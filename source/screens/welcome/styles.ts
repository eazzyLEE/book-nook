import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  background: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  leadText: {
    fontSize: 35,
  },
  subtext: {
    fontSize: 18,
    marginTop: 10,
  },
  buttonView: {
    width: '100%',
    alignItems: 'center',
    position: 'absolute',
    bottom: 35,
  },
});
